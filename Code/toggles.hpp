#include "Loops.hpp"

namespace toggles {
	namespace self {
		extern bool godmode;
		extern bool never_wanted;
		extern bool superman;
		extern bool ragdoll;
		extern bool invisible;
		extern bool off_radar;
	}
	namespace recovery {
		extern bool stealth_loop;
	}
	namespace player {
		extern bool remote_cash_drop;
		extern bool spectate;
		extern bool off_radar;
		extern bool never_wanted;
	}
	namespace vehicle {
		extern bool spawn_inside;
		extern bool spawn_maxed;
	}
}
